apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kerberus-dashboard.app.fullname" . }}-app-config
  labels:
    {{- include "kerberus-dashboard.app.labels" . | nindent 4 }}
data:
{{ include "kerberus-dashboard.app.configFilename" . | indent 2 }}: |
{{ tpl (.Files.Get "files/app-config.yaml.tpl") . | indent 4 }}
