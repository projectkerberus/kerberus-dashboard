apiVersion: v1
kind: Service
metadata:
  name: {{ include "kerberus-dashboard.app.fullname" . }}
  labels:
    {{- include "kerberus-dashboard.app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: {{ .Values.app.container.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "kerberus-dashboard.app.selectorLabels" . | nindent 4 }}
